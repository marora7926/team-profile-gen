// Function that returns a Manager card if manager is selected
    function renderManagerProfile(profile1) {
        if (profile1 === "Manager") {
        return (
        ` `
        )}
        return ``
    }

    // Function that returns an Engineer card if engineer is selected
    function renderEngineerProfile(profile2) {
        if (profile2 === "Engineer") {
        return (
        ` `
        )}
        return ``
    }

    // Function that returns an Intern card if intern is selected
    function renderInternProfile(profile3) {
        if (profile3 === "Engineer") {
        return (
        ` `
        )}
        return ``
      }

// Function to generate team profile HTML
function generateTeamHTML(answers) {
    return ` `;
}

module.exports = generateTeamProfile;